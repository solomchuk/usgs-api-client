#-------------------------------------------------------------------------------------------
# Author - Max Solomcuk, max.solomcuk@cgi.com
# YAML schema for the cleardownloads request to the USGS Inventory API
# See https://earthexplorer.usgs.gov/inventory/documentation/json-api?version=1.4.1#cleardownloads
# for a description.
#-------------------------------------------------------------------------------------------

# The only parameter is a list of label strings (per user).
# The parameter is optional (empty yaml file will pass validation).
# List items must be strings. None/Null values not permitted - use empty string if needed.
labels: list(str(none=False), required=False)