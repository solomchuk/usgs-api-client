#-------------------------------------------------------------------------------------------
# Author - Max Solomcuk, max.solomcuk@cgi.com
# Parameters for the datasetfields request to the USGS Inventory API
# See https://earthexplorer.usgs.gov/inventory/documentation/json-api?version=1.4.1#datasetFields
# for a description.
#-------------------------------------------------------------------------------------------

# Use the datasetName from datasets response.
# The parameter is required and must be a valid string.
# Currently all USGS datasets have a name length of at least 4 characters.
datasetName: str(required=True, none=False, min=4)